<!DOCTYPE etl SYSTEM "http://scriptella.org/dtd/etl.dtd">
<!-- Both actions use one Scriptella transaction on the destination connection. -->
<etl>
    <connection id="destination"
                driver="mariadb"
                url="${destination.url}"
                user="${destination.user}"
                password="${destination.password}"/>

    <script connection-id="destination">
        INSERT INTO etl_destination
            (id, amount, description, unicode_text, nullable_value, happened_at)
        VALUES (999, 9.99, 'must roll back', 'ロールバック', NULL, '2025-01-02 03:04:05')
    </script>

    <script connection-id="destination">
        INSERT INTO etl_missing_table (id) VALUES (999);
    </script>
</etl>
